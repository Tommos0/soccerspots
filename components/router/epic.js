// import { combineEpics } from 'redux-observable';
// import * as actions from './actions'
//
// export default combineEpics (
//     (action$, store) => action$.filter(action.type === actions.setPage.name).map(action => {
//         console.log(action); return {type:'epic in router'};
//     })
// );
